const BASE_URL = 'https://127.0.0.1:3000';
// Authentication
const LOGIN_URL = `${BASE_URL}/users/login`;
const LOGOUT_URL = `${BASE_URL}/users/logout`;
const REGISTER_URL = `${BASE_URL}/users/create`;

// Spotify
const SEARCH_URL = `${BASE_URL}/api/search`;
const TRACKS_URL = `${BASE_URL}/api/tracks`;

// Playlists
const PLAYLISTS_URL = `${BASE_URL}/api/playlists`;

// Liked Songs
const LIKED_SONGS_URL = `${BASE_URL}/api/liked-songs`;

// Archive
const ARCHIVE_URL = `${BASE_URL}/api/archives`;

// Friends
const FRIENDS_URL = `${BASE_URL}/api/users/friends`;

// Users
const USERS_URL = `${BASE_URL}/api/users`;

// Messages
const MESSAGES_URL = `${BASE_URL}/api/messages`;

// Logged into spotify account
const SPOTIFY_LOGGED_IN = `${BASE_URL}/api/spotify`;