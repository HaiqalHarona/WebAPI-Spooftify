const mongoose = require('mongoose');
const chat = require('../models/archivedchat.js');

let archivedchatservice = {
    async sharesongurl(sender, receiver, trackurl) {
        try {
            // Create chat message object
            
        } catch (error) {
        }
    }
};

module.exports = archivedchatservice;